#!/bin/sh
set -eu
ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PYTHON="$ROOT_DIR/.venv-core/bin/python"
if [ ! -x "$PYTHON" ]; then
  PYTHON=/usr/bin/python3
fi
exec "$PYTHON" "$ROOT_DIR/pipeline.py" "$@"
